@{
    ViewBag.Title = "Reports | Request Bulk Bills";
    Layout = "~/Views/Shared/Layout/_adminSchLayout.cshtml";
}


<ul class="breadcrumb">
    <li class="breadcrumb-item"><a href="#">Reports</a></li>
    <li class="breadcrumb-item"><a href="@Url.Action("RequestBulkBills","SchReports")"> Request Bulk Bills </a></li>
</ul>

<div class="content-i" id="app">
    <div class="content-box">
        <div class="element-wrapper">
            <h6 class="element-header"> Request Bulk Bills </h6>
            <div class="element-box">
                <div class="table-responsive" style="height:auto;width:auto;min-height:500px;">

                    <div class="row">

                        <div class="col-sm-1">  </div>

                        <div class="col-sm-3">

                            <div class="form-group">
                                <label> <b> Academic Year: </b>  </label>
                                <select class="form-control mb-3" v-model="selectedAcademicYearId">
                                    <option value="">Select Academic Year</option>
                                    <option :value="academicYear.id" v-for="academicYear in academicYearList"> {{academicYear.name}} </option>
                                </select>

                            </div>

                            <div class="form-group">
                                <label> <b> Term: </b>  </label>
                                <select class="form-control mb-3" v-model="selectedTermId">
                                    <option value="">Select Term </option>
                                    <option :value="term.id" v-for="term in termList"> {{term.name}} </option>
                                </select>

                            </div>


                            <div class="form-group">
                                <label> <b> Class: </b>  </label>
                                <select class="form-control mb-3" v-model="selectedClassId">
                                    <option value="">Select Class</option>
                                    <option :value="schClass.id" v-for="schClass in classList"> {{schClass.className}} </option>
                                </select>

                            </div>

                            <button type="button" :disabled="!selectedAcademicYearId || !selectedTermId || !selectedClassId || isLoading" v-on:click="requestBulkBills()" class="alignright btn btn-primary mt-3">
                                Request
                               
                                <i class="fa fa-spinner fa-spin" v-if="isLoading"></i>

                            </button>

                        </div>

                    </div>

                </div>

            </div>
        </div>


    </div>
</div>

<script src="~/js/school-manager/reports/request-bulk-bills.js" asp-append-version="true"></script>